# Ralph Progress Log — DdC Fase 11: Verifica E2E browser (Playwright)
# Avviato: 2026-06-26

## Contesto
- Fase 11: 11 storie (US-1101..US-1111) — test end-to-end nel browser con Playwright.
- branchName: ralph/fase-11-e2e-browser (basato su origin/master 054ae26, ultimo codice).
- Obiettivo: verificare che TUTTI i percorsi utente del sito funzionino (correttezza + usabilita), non aggiungere feature.
- Le fasi 1-10 (backend + frontend mappa/coniuge/PDF/accertamento) sono gia implementate e committate.

## Infrastruttura gia pronta (US-1101 scaffoldata e verificata localmente)
- pyproject.toml: aggiunto 'playwright>=1.40' al gruppo dev + marker 'e2e'.
  NB: NON usare pytest-playwright (API sync/greenlet): confligge con asyncio_mode=auto
  e lascia un event loop "running" che rompe i test backend async.
- tests/e2e/conftest.py:
  - fixture 'live_server' = uvicorn in-process (thread) -> base URL.
  - fixture function-scoped autouse '_stub_geo' = stub deterministico di
    resolver._swisstopo.resolve / _nominatim.resolve / tp_proximity.find_nearest_stop,
    con RIPRISTINO dopo ogni test (vedi lezione sotto).
    Tabella coordinate: 6500 Bellinzona, 6900 Lugano, 6600 Locarno, 6830 Chiasso,
    6512 Giubiasco (<5km da Bellinzona, per test blocco auto), 22100 Como (IT).
  - fixture async 'page' = Chromium via async_playwright (function-scoped).
- tests/e2e/helpers.py: funzioni ASYNC: set_city (TomSelect create:true), fill_address,
  fill_minimal_addresses, calcola (attende "Cantonale TI" nei risultati), results_text.
- tests/e2e/test_smoke.py: 2 test async (homepage + card principali) -> PASS su chromium.
- Verificato localmente: 'uv run pytest tests/ -q' = 133 passed (131 backend + 2 e2e), 0 falliti.

## Note importanti per chi implementa le storie
- PRIMA dei test eseguire 'uv run playwright install chromium' se i browser mancano.
- I test E2E sono ASYNC (playwright.async_api), coerenti con asyncio_mode=auto del progetto.
- LEZIONE: lo stub geo patcha lo stato globale del modulo. DEVE essere function-scoped e
  ripristinare in finally, altrimenti (session-scope) resta attivo e fa fallire i test
  backend integration con 422 (override distanza vs distanza stubbata).
- Le citta usano TomSelect con create:true: digitare il nome + Invio; impostare l'NPA
  DOPO la citta (onItemAdd puo sovrascriverlo se vuoto). Vedi helpers.set_city.
- I km sono deterministici grazie allo stub geo (haversine reale su coordinate fisse).
- /v1/locations/search e /v1/locations/npa restano esterni: con create:true non servono,
  ma se necessario intercettarli con page.route.
- OSRM (router.project-osrm.org) e esterno: per US-1109 usare page.route o asserire solo
  presenza mappa + marker, senza dipendere dal tracciato.
- PDF (jsPDF) generato lato browser: per US-1110 usare page.expect_download.
- Lo stub tp_proximity.find_nearest_stop ritorna None (nessuna fermata vicina): il blocco
  auto privata <30km va testato con la coppia Bellinzona->Giubiasco (distanza breve).

## Stato storie
- US-1101..US-1111: passes=false (US-1101 gia pronta a livello di codice, da confermare in ambiente ralph).
